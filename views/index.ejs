<% include ./partials/header %>
<h1>So Much!!!</h1>

<% blogs.forEach(function(blog){%>
    <div>
        <h2><%= blog.title %></h2>
        <img src="<%=blog.image%>">
        <p><%= blog.body%></p>
        <span><%=blog.created%></span>
    </div>
    
<% }) %>

<% include ./partials/footer %>